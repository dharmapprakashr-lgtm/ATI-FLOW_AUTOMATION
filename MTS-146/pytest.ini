[pytest]
markers =
    p0: Critical / P0 priority test cases
    p1: High / P1 priority test cases
    staging_area: Staging Area tab tests
    processing_area_config: Processing Area Config tab tests
    containers: Containers tab tests

addopts = -v --html=reports/report.html --self-contained-html
testpaths = tests
